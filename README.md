Style-Coding-Manager
====================

The repository where I put all the Style Coding Manager I wrote

#Branching Hierarchy

##master

Only push on master scm that are validate, until validation, work on branches

##branches

work on them to edit the scm
